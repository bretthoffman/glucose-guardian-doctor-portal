# Copy to .env.local for local development. Do NOT commit .env.local.

# --- Clerk (shared application) ---
# The publishable key is public and safe to commit in this example file.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_bmV4dC1vc3ByZXktMTUuY2xlcmsuYWNjb3VudHMuZGV2JA
# NOTE: this is a static SPA with no server runtime, so CLERK_SECRET_KEY is NOT used
# here and must never be added to a VITE_ variable. Server-side Clerk work belongs in
# the Convex backend.

# --- Convex (shared deployment: clean-ptarmigan-904) ---
VITE_CONVEX_URL=https://clean-ptarmigan-904.convex.cloud
# Only needed if the portal calls Convex HTTP actions:
# VITE_CONVEX_SITE_URL=https://clean-ptarmigan-904.convex.site

# --- Legacy REST API (being replaced by Convex; still used by current panels) ---
VITE_API_BASE_URL=https://glucose-guardian-ashen.vercel.app
VITE_API_PROXY_TARGET=http://localhost:5173
